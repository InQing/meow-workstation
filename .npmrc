# 本仓库 package-lock.json 的 tarball 全部指向 npmmirror；
# npm 12 默认 registry 是 npmjs.org，会把「主机不一致的绝对 URL」判定为 remote 包并拒绝
# （EALLOWREMOTE / allow-remote=none）。让 registry 与 lock 对齐即可正常安装。
registry=https://registry.npmmirror.com
electron_mirror=https://npmmirror.com/mirrors/electron/
