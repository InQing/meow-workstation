# 后期计划

> 记录「还有什么没做、为什么、做到什么程度算完」。**改需求时先来这里对一下**，避免重复决策。

---

## 现状

核心玩法已闭环，`npm start` 可跑，已推 GitHub。阶段全景：

| 阶段  | 内容                              | 状态 |
| --- | ------------------------------- | -- |
| 1–2 | 桌宠窗口、像素猫渲染、状态机、拖动与缩放            | ✅  |
| 3   | 番茄钟（主进程时间源 + 三路同步 + 托盘 tooltip） | ✅  |
| 4   | 好感养成（摸头 / 喂食 / 等级解锁）            | ✅  |
| 5   | 面板 + 集卡（17 卡图鉴、抽卡保底、设置）         | ✅  |
| 6   | 视觉改造：复古街机 CRT                   | ✅  |
| 7   | 游戏中心 + 鱼干突袭                     | ✅  |
| 8   | 反应力测试；番茄进度条 → 左侧沙漏              | ✅  |
| 9   | 自定义图片桌宠（默认图 + 9 状态槽位 + 选宠入口）     | ✅  |
| —   | 项目命名、README、架构文档、推 GitHub       | ✅  |

**没做完的共同点**：大部分是「等素材」或「等打包」，功能层面基本没有阻塞项。

---

## 后期需求-优先级总览

| 优先级    | 事项                                | 阻塞谁        | 工作量        |
| ------ | --------------------------------- | ---------- | ---------- |
| **P0** | [卡面图案接入]            | 集卡玩法的"完成度" | 中（接口已预留）   |
| **P0** | [内置猫换成正式图案]（选宠 / 上传入口已随图片桌宠落地） | 桌宠颜值    | 中（等素材） |
| **P1** | [透明区点击穿透]         | 日常使用手感     | 小（接口已预留）   |
| **P1** | [打包成 exe]         | 给别人用       | 中          |
| **P2** | [内容扩展]                | 留存         | 大          |
| **P3** | [工程债清理](#已知工程债)                   | 维护成本       | 小          |

---

## 已知工程债

| # | 问题                                                                                            | 影响                                                  | 修法                                     |
| - | --------------------------------------------------------------------------------------------- | --------------------------------------------------- | -------------------------------------- |
| 2 | **`audio.js` 保留但零调用**                                                                         | 低（注释已说明），但容易让新人以为可以去用                               | 要么删，要么在文件头明确标「预留，勿调用」                  |
| 3 | **桌宠 hover 判定是整个窗口** —— 左留白加宽后透明区更大                                                           | 鼠标从猫左侧划过容易误弹按钮栏                                     | 与 P1-1 的点击穿透一起做，收敛到猫身轮廓                |
| 4 | **`MGW_` 前缀与项目名不一致** —— MGW = MiaoGongWei，现已改名 meow-workstation，40+ 处                         | 纯观感问题                                               | **刻意不改**：内部符号，收益低、漏改即崩                 |
| 5 | `tools/_smoke/` 截图产物无清理机制                                                                     | 低，已被 `.gitignore`                                   | 加个 `npm run clean:smoke`               |
| 6 | `assets/ref/` 存着早期 AI 生图参考（实测 248KB）                                                          | 无                                                   | 已随仓库公开；不想要就删                           |

已还清的债（留档，避免重复发现）：

| 问题                                                                         | 修法                                    | 时间         |
| -------------------------------------------------------------------------- | ------------------------------------- | ---------- |
| `pngjs` 是未声明的隐式依赖 —— `tools/png2grid.js`、`tools/render-preview.js` require 它，但依赖表里只有 electron（新克隆后这两个工具直接报错） | 写进 `devDependencies`（`^7.0.0`），lock 同步 | 2026-09-11 |
| `DEFAULT_SAVE` 与运行时键不同步（`settings.petScale`、`stats.bestReflexMs`、`stats.reflexBreaks` 没进默认档，编号 1 的空位就是它） | 补进 `DEFAULT_SAVE` + 新增 `currentPet`；`tools/save-selftest.js` 盯住 | 2026-09-11 |

---

## 待拍板

需要老大决定的开放问题：

1. **要不要做云存档 / 多设备同步？** —— 现在是纯本地单机。
2. **有没有打包分发的目标平台？** —— 只 Windows，还是也要 macOS。
3. **图片桌宠要不要动效 / GIF？** —— v1 静态单图，`pet.json` 的槽位结构已留扩展位（见 `docs/superpowers/specs/2026-09-11-image-pet-design.md`）。
